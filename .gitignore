# Ignore everything:
*

# Except for these files:
# Re-include all directories (& sub-dirs) so they may be examined for the exceptions that follow
!*/
# Don't ignore the .gitignore!
!.gitignore
# Dockerfile
!Dockerfile
# Text files
!*.txt
# Markdown
!*.md

.ipynb_checkpoints
